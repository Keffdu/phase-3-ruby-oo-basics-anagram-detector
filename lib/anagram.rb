class Anagram
    attr_reader :book

    def initialize(word)
        @word = word

    end

    def match(array)
        # return all matches in the array,
        # if not matches - return empy array
    end
end